<div
  class="flex flex-grow flex-col overflow-hidden items-center justify-center p-[1rem] pb-[max(1rem,_env(safe-area-inset-bottom))] pt-[max(1rem,_env(safe-area-inset-top))] min-h-full"
>
  <div
    class="max-w-[375px] w-full flex flex-col flex-grow justify-center px-[1px] overflow-hidden lg:flex-grow-0"
  >
    <slot />
  </div>
</div>
